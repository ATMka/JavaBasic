package Warmup1;

public class SleepIn {
    public static boolean go(boolean weekday, boolean vacation) {
            if (!weekday || vacation) {
                return true;
            }

            return false;
    }
}
